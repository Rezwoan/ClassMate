name: Deploy to Raspberry Pi

on:
  push:
    branches: [main]
  workflow_dispatch:

# Never run two deploys at once; let an in-flight one finish.
concurrency:
  group: classmate-deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: Build & Deploy
    # Targets the dedicated ClassMate runner on the Pi (label set at registration).
    runs-on: [self-hosted, classmate]
    steps:
      - name: Deploy
        run: bash /var/www/classmate/scripts/deploy.sh
